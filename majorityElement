class Solution(object):
    def majorityElement(self, nums):
        nums.sort()
        a=1
        for i in range(1,len(nums)):
            if nums[i]==nums[i-1]:
                a+=1
            else:
                a=1
            if a>(len(nums)/2):
                return nums[i]
        return nums[0]
        
