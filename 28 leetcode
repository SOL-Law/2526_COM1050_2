class Solution(object):
    def strStr(self, haystack, needle):
     a=len(haystack)
     b=len(needle)
     for i in range(a-b+1):
        catchuoi=haystack[i:i+b]
        if catchuoi==needle:
            return i
     return -1
