class Solution(object):
    def singleNumber(self, nums):
        a=set()
        for i in range(len(nums)):
         if nums[i] in a:
            a.remove(nums[i])
         else:
            a.add(nums[i])
        return   a.pop()
